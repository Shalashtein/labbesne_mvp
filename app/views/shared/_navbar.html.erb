<nav class="navbar navbar-expand-lg">
  <a class="navbar-brand" href="/">LABBESNE</a>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"><i class="fa fa-bars"></i></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ms-auto">
      <% if spree_user_signed_in? %>
        <% if current_spree_user.spree_roles.empty? %>
          <li class="nav-item">
            <%= link_to "Account", customer_dashboard_path, class: "nav-link" %>
          </li>
        <% elsif current_spree_user.has_spree_role?(:stylist) %>
          <li class="nav-item">
            <%= link_to "Stylist Dashboard", stylist_path, class: "nav-link" %>
          </li>
        <% elsif current_spree_user.has_spree_role?(:vendor) %>

        <% elsif current_spree_user.has_spree_role?(:admin) %>
          <li class="nav-item">
            <%= link_to "Admin Dashboard", spree.admin_products_path, class: "nav-link" %>
          </li>
        <% end %>
      <li class="nav-item">
        <%= link_to "Logout", spree.logout_path,method: :delete, class: "nav-link" %>
      </li>
      <% else %>
      <li class="nav-link">
        <%= link_to "Login", spree.login_path, class: "nav-link" %>
      </li>
      <% end %>
    </ul>
  </div>
</nav>
